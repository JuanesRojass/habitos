import 'package:flutter/material.dart';

void main() => runApp(const ListaHabitosScreen());

class ListaHabitosScreen extends StatelessWidget {
  const ListaHabitosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("");
  }
}